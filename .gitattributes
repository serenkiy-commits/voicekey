# Шелл-скрипты для macOS/Linux всегда с LF (иначе ломается shebang и bash)
*.sh        text eol=lf
*.command   text eol=lf

# Скрипты Windows с CRLF
*.bat       text eol=crlf
*.ps1       text eol=crlf
